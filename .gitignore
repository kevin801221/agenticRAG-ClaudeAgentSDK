.venv/
__pycache__/
*.pyc
.env
data/
.pytest_cache/

# Jupyter 的自動存檔副本。進了 repo 只會變成「有兩份 notebook，改到錯的那份」
.ipynb_checkpoints/

# 論文 PDF 不進 repo（別人的著作，各自有授權）—— 用 scripts/fetch_papers.sh 抓
corpus/papers/

# 筆記是個人的，不進 repo（要分享就用匯出的 markdown）
notes/

# 上傳的文件是使用者自己的，不進 repo
corpus/uploads/

# 自訂架構**要**進 repo —— 學生聊出來 / 拖出來的架構就是這堂課的產出，
# 也是最值得互相看的東西。自己實驗用的丟 architectures/local/。
architectures/local/

# 軌跡是跑出來的紀錄，預設不進 repo（要當教材就手動加想留的那幾份）
traces/
mcp.json
